# Fake Firebase credentials for testing purposes
FIREBASE_PROJECT_ID=test-project-id
FIREBASE_CLIENT_EMAIL=test-service-account@test-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nFAKEPRIVATEKEY1234567890\n-----END PRIVATE KEY-----\n"

# Optional email configuration if your app sends emails during tests
EMAIL_USER=test@example.com
EMAIL_PASS=testpassword

# Swagger server (if needed)
SWAGGER_SERVER_URL=http://localhost:3000/api/v1

# Optional: override port for testing
PORT=4000

# Explicit test environment
NODE_ENV=test
